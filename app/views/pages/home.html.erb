


<div class="main">
  <div class="row">
      <% @projects.each do |p| %>
        <div class="col-6">
          <div class="project-card">
            <%= image_tag "#{p.image_url}", height: "400px" %>
            <br>
              <div class="project-card-text">
              <h5> <%= p.title %> </h5>
              <p> <%= p.description %></p>
            </div>
          </div>
        </div>
      <% end %>
  </div>
</div>
